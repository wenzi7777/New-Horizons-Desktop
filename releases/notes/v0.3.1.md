# New Horizons Desktop v0.3.1

## Changes

### File Upload Cancel

- Added a **Cancel** button that appears in the upload progress bar while a file
  upload is in progress. Clicking it aborts the transfer at the next chunk
  boundary and reports `uploadFailed: upload_cancelled`.
- Chunk size remains 96 bytes (unchanged, preserving network-layer MTU safety).
